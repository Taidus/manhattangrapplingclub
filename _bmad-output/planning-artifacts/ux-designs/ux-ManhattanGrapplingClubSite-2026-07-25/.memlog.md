---
topic: Manhattan Grapple Club UX spines
updated: 2026-07-26T16:09
---

- (event) Palette sampled from badge pixels via BMP conversion + dominant-colour analysis, not eyeballed. Gold=#FDB71B, Red=#E81D2E.
- (decision) Design is DARK-surface-first. Forced by measurement: gold-on-white is 1.76:1 (unusable), gold-on-ink is 11.21:1. Also matches badge construction and the sport.
- (decision) Primary button = gold fill + ink label (11.21:1). Highest-contrast on-brand pairing available.
- (decision) Red restricted to large text / small accents only -- 4.35:1 on ink fails body contrast.
- (decision) IA ordered to the user's stated priority: schedule, trial, cost, location, instructor. Not marketing-site convention.
- (decision) Women's BJJ gets a dedicated block directly under the schedule, not a table row.
- (decision) Community Standards content (recovered from PDFs) promoted to a full section -- consent/refusal/zero-tolerance language does the trust work the missing instructor bio cannot.
- (decision) System font stack for body; no webfont bundled, keeping the draft build-free and network-free. Display face is a documented production upgrade.
- (decision) Motion: enters ease-out, exits ease-in and faster; transform/opacity only; nothing over 400ms; scroll-reveal must be self-healing (visible by default).
- (assumption) Archivo Black named as display face but not bundled; falls back to Helvetica/Arial black weights in the draft.
- (event) All contrast pairs in DESIGN.md computed programmatically against actual token values.
- (event) Spines written: DESIGN.md + EXPERIENCE.md
- (event) Spines finalized; build verified against them (no-JS, reduced-motion, 4 breakpoints, tap targets).
- (event) Craft review run via review-animations + emil-design-eng skills; verdict BLOCK with 4 blocking findings.
- (change) Display face switched from unloaded Archivo Black to system stack @800 -- fallback stack lacked system-ui, so iOS synthesised 900 on Helvetica Neue and smeared every heading.
- (change) Hero CTAs stacked full-width below 480px -- they wrapped and sat flex-start, left-aligned under centred hero text. Real visible bug, confirmed in screenshot.
- (change) placeholder-label red #E81D2E on charcoal measured 3.46:1 at 16px bold (needs 4.5). Added --red-hi #FF4D5A at 4.81:1. Original code comment had asserted a non-existent WCAG exemption.
- (change) Scroll reveal reduced to opacity-only 220ms (was fade+12px rise @400ms); removed nested .feature-inner target; safety-net no longer silently disables the effect.
- (change) Removed both backdrop-filters and the hero badge drop-shadow -- invisible on near-black, but re-composite every scroll frame.
- (change) Badge re-encoded 984px->384px via Chrome canvas (84KB->42KB) plus an 18KB 192px variant for header/footer.
- (decision) Kept scroll-behavior:smooth despite review flag -- conventional, and already disabled under reduced-motion. Taste, not defect.
- (event) Re-verified after fixes: no overflow 320/390/768/1280, zero sub-44px targets, hero fits fold at 811px, 0 backdrop-filters, no layout-triggering transitions, max duration 260ms.
- (change) Schedule rebuilt as a calendar on user feedback ('layout is confusing'). Root cause: showing only the 5 class days made Thu->Sun read as a gap, not a closed weekend.
- (change) All 7 days now render Mon->Sun including Fri/Sat as explicit 'Closed' cells.
- (change) Two views of one markup: agenda below 1024px, true 7-column week grid at 1024px+. Grid waits for 1024 because a seventh of the width below that is <95px.
- (change) Added aria-hidden week-at-a-glance strip below 1024px -- carries the calendar shape where the grid cannot fit; next-class mark mirrored onto it.
- (change) Tag copy shortened ('Best for day one'->'Start here', 'Free - everyone'->'Free') -- both wrapped to two lines inside a 144px calendar column.
- (change) Header made fully opaque; at 0.97 alpha hero text was legible through it while scrolling.
- (event) Re-verified: no overflow 320-1280, 0 clipped glance chips at 320, 0 sub-44px targets, JS-off renders all 7 days incl. Closed, reduced-motion hides nothing.
- (change) Added mobile menu -- below 768px there was NO navigation at all: .site-nav was display:none and .header-cta was hidden too.
- (decision) Built on native <details>/<summary> so it works fully with JS off; JS adds only Escape, outside-click, and close-on-navigate.
- (change) BUG FOUND+FIXED: native <details> hiding does not survive an absolutely-positioned display:grid child -- panel rendered permanently over the page (290x478, links visible). Added explicit .menu:not([open]) .menu-panel { display:none }.
- (change) BUG FOUND+FIXED: .menu-panel a (0,1,1) out-specified .btn-primary (0,1,0) on color, painting the menu CTA white-on-gold = 1.76:1, the banned pairing. Matched specificity with .menu-panel a.menu-cta; now 11.21:1.
- (event) Menu verified: 44x44 button, 48px+ link rows, panel inside viewport, Escape/outside-click/link-click all close, 9 links present with JS disabled.
- (change) Hero copy replaced with client-supplied text (Curious about jiu-jitsu? / You Don't Have to Be Ready to Start / no-experience paragraph).
- (decision) New copy carries neither the city nor the trial offer, both FR-1 above-fold requirements -- relocated into the hero fact chips rather than dropped. Verified all four FR-1 items still above the fold at 320/390/1280.
- (change) Instructor placeholder reworded to 'every coach' -- hero now promises 'our coaches' (plural), so the naming gap widened.
- (event) Client supplied the three policy handouts as images -- far richer than the PDF text extraction (headline + reasoning + bolded key phrases per item).
- (change) Standards section rebuilt to mirror the handouts: 8 numbered standards, 5+5 Do/Don't sparring rules as cards, 4 hygiene groups. 38 bolded key phrases.
- (decision) Do/Don't uses gold and red rather than the handouts' green/red -- keeps the palette to its existing hues. Both are large text where red clears AA; verified redHi-on-ink 6.05, gold-on-ink 11.21.
- (decision) Emphasis carried by CONTRAST (bold steps --muted up to --white at 17.89:1), not by an extra colour, so 38 highlights don't turn into visual noise.
- (event) FOUND ERROR IN CLIENT MATERIAL: Safety & Hygiene handout prints the 911 address ZIP as 65502; Manhattan KS is 66502. Site uses 66502; handout needs correcting at source.
- (change) Removed the standards section lede ('rules of the room / used to be three PDF downloads') per user request.
- (change) Community standards boxed as cards to match the sparring and hygiene groups; 2 columns from 860px.
- (change) BUG FOUND+FIXED: making .std-list li a grid container turned every text run and <strong> into a separate grid item, shredding copy to one fragment per cell. Numeral is absolutely positioned instead; li stays normal flow.
- (decision) Added a chars-per-rendered-line check to catch shredded text layout -- structural checks (bg/border/columns) passed while the render was broken. Count DISTINCT line tops, not getClientRects().length, since every inline <strong> adds a rect on the same line.
- (change) Three client photos placed by their labels: gym photo -> Location, womensbjj class -> Women's BJJ, coach hunter -> Who you'll train with.
- (change) Photos re-encoded to WebP at two widths each via Chrome canvas and served with srcset/sizes; JPEG sources deleted after encoding. Mobile path is 182KB across all three, all lazy-loaded below the fold.
- (decision) Coach section now leads with 'Coach Hunter' (name supplied by the client in chat, a valid source) and keeps a placeholder for belt rank, lineage, bio.
- (decision) Did NOT state which of the two people in the coach photo is Hunter, nor either person's belt rank -- the photo shows two practitioners and nothing identifies them. Placeholder asks the client to confirm.
- (event) Gym photo corroborates the co-location assumption: CROSSFIT 785 and 'BURN THE SHIPS' signage visible in the club's own training space. Wayfinding placeholder kept -- still no door/parking instruction.
- (event) Regression after photos: no overflow 320-1280, 0 sub-44px targets, 0 missing alt, 0 shredded text, JS-off and reduced-motion unchanged.
- (change) Gym photo moved from the Location section to a full-bleed hero background; the Location figure and its two crops were removed so the image isn't used twice.
- (decision) Hero scrim tuned by measurement, not by eye: render with hero text hidden, screenshot, sample the BRIGHTEST pixel under each text block, check against that text's colour.
- (event) First lighter scrim FAILED on the 12px gold eyebrow at 1280 (4.17 vs 4.5) while every white element passed. Gold is the binding constraint; darkened the radial centre 0.22->0.38 and all pass with margin.
- (decision) Hero lede/facts moved off --muted to near-white (#EDEDF0/#E2E2E7). Mid-grey has no contrast headroom over a photo and would have forced a scrim so dark the photo vanished.
- (decision) Hero images encoded at q=0.62 because the scrim hides artefacts; mobile hero is 44KB and is the LCP element.
- (change) Removed fetchpriority=high from the hero badge -- two images competing for LCP priority. The background keeps it.
- (change) Women's BJJ section modernised: time moved onto the photo as a floating gold chip, lede enlarged, the supporting paragraph became three scannable check-chips.
- (change) Trailing community-standards paragraph replaced with a link card ('Curious about our community standards?') -- full-row target, own surface, arrow that slides 4px on hover.
- (change) BUG FOUND+FIXED (third instance of the same trap): .photo figcaption (0,1,1) out-specified .photo-chip (0,1,0), so the gold time chip rendered as the default grey caption bar. Fixed with .photo figcaption.photo-chip.
- (decision) Specificity collisions with base component rules are now a recurring failure mode in this stylesheet (menu CTA, photo chip). Variant classes on an element that already has a descendant-selector rule MUST match or exceed that specificity.
- (event) Contrast verified for all new surfaces: linkcard title 8.91, sub 6.26, hover-state 8.56/6.01, photo chip 11.21, feature chips 17.89.
- (event) Restyled to the CollegiateClub direction from the user's Claude Design system project (templates/collegiate-club). Sections and content unchanged.
- (decision) Adopted: Archivo Black display at weight 400 (never higher -- single-weight family), hard corners everywhere except 2px on photos, 6px gold rules as structure, numbered section headers, panel-grid hairline technique, numeral-forward scale.
- (decision) ACCEPTED TRADE-OFF: the page now makes a Google Fonts request, ending the previous build's network-free property. Mitigated with preconnect + display=swap. Self-hosting woff2 is the fix if offline matters.
- (change) FIXED 2 AA FAILURES INHERITED FROM THE TEMPLATE: closed-day text #4E4E58 measured 2.39:1 and rest-day header #6A6A74 measured 3.50:1. Raised to #7C7C87 (4.77) and #82828D (4.93). 'CLOSED' is real information, not decoration.
- (change) BUG FOUND+FIXED (4th specificity instance): .hero-bg img (0,1,1) out-specified .hero-portrait (0,1,0), so BOTH hero crops rendered stacked at every width. Now .hero-bg img.hero-portrait.
- (change) Hero art-direction pair added: 3:4 portrait crop below 1024px, gated on geometry (960px = 720 x 1.333) not a round breakpoint.
- (change) Next-class marker moved into the day's gold header bar as a 'Next' / 'On now' label; scroll-reveal targets updated from .wrap > h2 to .secthead.
- (event) Regression: no overflow 320-1600, 0 sub-44px targets, 0 stray border-radius outside photos, JS-off renders 7 days + 6 placeholders + 9 menu links, reduced-motion hides nothing, Archivo Black confirmed loaded at weight 400.
- (event) PRICING RESOLVED: client supplied Glofox screenshot. Founding Member $85/mo (first 20 members only), Women's Only $50/mo, 2-week trial free. Highest-priority gap closed.
- (decision) Prices published exactly as the portal states them -- no rounding, no 'from $50', no inferred annual equivalent.
- (change) Added priceRange $50-$85 and currenciesAccepted USD to the LocalBusiness JSON-LD, now that both are verified. Phone/email/rating still absent.
- (decision) Replaced the membership-pricing placeholder with a narrower founding-availability one: the plan advertises a 20-member cap, and a stale scarcity claim is worse than no claim.
- (event) FOUND TYPO IN CLIENT MATERIAL: the Glofox plan is spelled '2-Week Free Trail' (trail, not trial) in both the heading and the card -- on the page the site's primary CTA sends customers to. Only the club can edit it.
- (override) User directed removal of the founding-availability placeholder. Concern was raised (a 20-member cap published without verification becomes a promise the club may not be able to honour) and the user decided; removed. The 'first 20 members' claim now stands on the page untracked. Placeholders 6 -> 5.
- (event) CONTACT RESOLVED: client supplied phone +1 620 491 8278 and email info@manhattangrappleclub.com. Rendered as tel:/mailto: rows in Finding Us plus the footer. Placeholders 5 -> 4.
- (change) Added telephone and email to the LocalBusiness JSON-LD. aggregateRating and openingHours still deliberately absent.
- (decision) Email exempted from the direction's uppercase display treatment: an address is data, not a label, and rendering INFO@... implies the address is uppercase. Switched to body face, sentence case, with overflow-wrap:anywhere so 32 chars never clip at 320px.
- (event) CO-LOCATION CONFIRMED by the client: classes are held inside the 785 Crossfit facility. The long-standing [ASSUMPTION] tag is retired.
- (change) WAYFINDING RESOLVED: exterior photo added to the Finding Us section plus a 3-step 'Getting in' list (share the building / look for the CrossFit 785 + 785 Athletics signs and the yellow metal building / park out front, entrance under the covered walkway). Every detail is visible in the supplied photo. Placeholders 4 -> 3.
- (change) BUG FOUND+FIXED: .find-steps li was display:flex and its <strong> elements each became a separate flex item -- shredding item 2 to 10 lines for 105 chars AND, because flex items could not shrink, forcing the grid track to 321px in a 320px viewport. ONE root cause, TWO symptoms. Switched to an absolutely-positioned counter.
- (decision) Flex/grid on a list item is only safe when the item's content is PLAIN TEXT. .ticks and .feature-chips are fine; anything containing <strong> needs the absolute-counter pattern. Third instance of this family (.std-list, .find-steps).
- (change) Removed the linkcard sub-line from Women's BJJ ('you set your own boundaries, you may decline any roll, harassment ends a membership') -- in a section meant to feel welcoming it read as a warning. Verified all five claims are already stated in section 06, so no content was lost.
- (change) BUG: .linkcard-title:last-of-type did not fire -- the arrow is also a <span>, so the title is never last-of-type, leaving 8px of dead bottom spacing. Replaced with :has(+ .linkcard-sub), which keeps the optional sub-line available and defaults the title to zero margin.
- (decision) Tone principle recorded: consent and zero-tolerance language belongs in Community Standards, where a reader has opted in to reading rules. Surfacing it as a teaser under a welcoming section inverts the intent.
- (change) Removed the Wednesday closing line and the whole community-standards link card from Women's BJJ per user request. Section is now eyebrow + h2 + lede + 3 numbered chips.
- (change) Deleted the now-dead .linkcard family (33 lines) and .feature-close from styles.css rather than leaving unused rules in a hand-maintained file. Spec retained in DESIGN.md as retired.
- (change) Added .feature-chips:last-child margin reset -- the chips became the last element in the column and would have left a 24px trailing gap.
- (event) Verified Standards is still reachable: 3 remaining #standards links (desktop nav, mobile menu, footer). Nothing orphaned by removing the in-section link.
- (event) MEASURED THE PROBLEM FIRST: the single page was 16511px at 390px wide = 19.6 phone screens, and the community standards began 9363px down. That is what the user was feeling.
- (change) Split into 4 pages: index (home, arrival moment), first-class (day one + FAQ), standards (conduct/sparring/hygiene), contact (NEW). Home is now 9361px -- 43% shorter -- and the first standard is 545px from the top of its own page.
- (decision) Content blocks were LIFTED VERBATIM from index.html by their comment markers via a one-time script, not retyped, so no copy could be silently altered or dropped in the move.
- (decision) Kept the no-build-step property: 4 plain static HTML files with duplicated chrome, guarded by tools/check-chrome.mjs (chrome parity + link integrity + orphan detection) rather than introducing a template build.
- (change) NEW contact.html: 4 large tappable reach cards (call/email/Instagram/Facebook), the full Find Us block with map + exterior photo + getting-in steps, and a class-times quick reference.
- (change) Home's Finding Us compacted to address + directions + phone + a link to contact.html; the map, exterior photo and wayfinding steps moved to the contact page.
- (change) Added .navcards on home so the three subpages are discoverable without the nav, and aria-current=page styling in both navs.
- (change) BUG FOUND+FIXED: standards.html skipped H1->H3 because the section h2 was removed with the secthead. Promoted .std-title to h2 and the rule/hygiene card titles to h3; CSS selectors updated.
- (change) check-chrome.mjs caught a stray _before.html temp file left in site/ -- a manual read would have missed it. Removed.
- (decision) Refined the shredded-text detector: raised the floor from 25 to 45 chars (a 33-char sentence legitimately wraps to 2 lines in a narrow card) and extended the inline-link tap-target exemption from p to p/li/address/figcaption per WCAG 2.2 SC 2.5.8.
- (event) Client identified Coach Hunter as the PURPLE BELT ON THE RIGHT of the photo. Both the which-person ambiguity and his belt rank resolved in one answer.
- (change) Published: alt text now names him and his position in the frame, caption reads 'Coach Hunter — right', and a purple belt rank indicator sits under his name. Placeholder narrowed to lineage/bio/other coaches.
- (decision) Ported the design system's BeltRank component to plain CSS using its own belt identity tokens.
- (event) The design system's warning on tokens/belts.css was CORRECT: --belt-purple measures 2.61:1 on --ink and the black rank bar is 2.61:1 on purple. Neither clears 3:1.
- (decision) Resolved by making the swatch decorative (aria-hidden) with the adjacent TEXT label carrying the rank. Lightening purple to pass a ratio would misrepresent an actual belt rank; removing the swatch would lose the sport's native vocabulary. Colour is never the sole carrier -- 'Purple belt' is in the DOM as text. A hairline border and inset light edges give the shape definition on a dark ground.
- (change) Added the contact form to contact.html as section 02 (Name/Email/Phone/Message), restoring what the original GoDaddy site had. Find Us renumbered to 03.
- (decision) Form ships UNWIRED with a launch-blocking placeholder rather than faked. A form that silently discards enquiries is worse than no form -- the sender believes they reached the club.
- (decision) method=post with no action, deliberately: a GET submit would put the visitor's name, email and phone into the URL. Never put personal data in a query string.
- (change) NEW TOKEN --field-border #6A6A74. --hairline-2 as a field border composites to ~#303032 = 1.49:1 against the field fill, and WCAG 1.4.11 wants 3:1 for a component boundary -- a text input's border IS its boundary. New token measures 3.68:1 on ink and 3.44:1 on the alt surface.
- (decision) Inputs at 16px minimum so iOS does not zoom on focus; visible labels never placeholder-as-label; :user-invalid rather than :invalid so empty required fields are not flagged before the visitor has typed.
- (decision) Did NOT add Google reCAPTCHA even though the original site had it -- it loads a third-party script and profiles visitors. Noted honeypot / service-side filtering as the alternative for the client to choose.
- (change) Form wired to mailto:info@manhattangrappleclub.com?subject=Website enquiry, with enctype=text/plain. Launch-blocker placeholder removed at user request; placeholders 4 -> 3.
- (decision) Kept method=post + enctype=text/plain rather than GET so the visitor's name, email and phone never enter a URL.
- (decision) Replaced the removed warning box with one honest line of microcopy ('Sending opens your email app') plus the address as a direct mailto link. No visible warning box, but the visitor is not misled about what the button does; the mailto caveat is recorded in an HTML comment and the README for whoever builds the final version.
- (event) MEASURED FIRST at 390px: 148px dead space above the hero badge, hero 983px tall in an 844px viewport, 176px between every section, doc 9250px.
- (change) ROOT CAUSE 1: .hero (64px) and .hero-inner (84px) both applied padding-block, stacking to 148px. .hero-inner now owns it alone via clamp(36px,7vw,84px).
- (change) ROOT CAUSE 2: --section-y had been flattened from the design system's clamp(56px,10vw,112px) to the template's flat 88px, charging mobile full desktop rhythm. Now clamp(44px,7vw,88px) -- 88px preserved from ~1260px up.
- (change) Clamped the whole top-of-page stack: badge size and margin, eyebrow, kicker, lede, btn-row, facts, secthead padding+margin, final-cta, pagehead, standards-grid gap.
- (change) Hero lede was a flat 19px at 1.6 line-height = 5 tall lines, the largest block above the fold. Now clamp(17px,4.4vw,19px) at 1.5, still above the 16px iOS-zoom floor.
- (event) RESULT at 390px: home 9250->8168px (-12%), hero 983->635px (now fits the viewport), badge dead space 148->36px, section gaps 176->88px. At 1280px: section padding still 88px, home 6324->6316px -- desktop unchanged.
- (event) Verified live after deploy: docH 8168, heroH 635, sectionPad 44px, badgeTop 36px, no overflow. All 4 pages clean at 320/390/768/1280 -- 0 sub-44px targets, 0 shredded text, 1 h1 each.
